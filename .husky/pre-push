echo "🔍 Running TypeScript build check before push..."

npm run build

if [ $? -ne 0 ]; then
  echo "❌ Build failed! Fix TypeScript errors before pushing."
  exit 1
fi

npm run test:e2e:prod || {
  echo "❌ End-to-end tests failed! Fix the issues before pushing."
  exit 1
}

echo "✅ Build successful! Proceeding with push..."
